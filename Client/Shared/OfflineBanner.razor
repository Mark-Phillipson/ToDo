@code {
    // Bound properties come from code-behind partial class
}
<div class="alert alert-warning py-2" role="alert" style="margin-bottom:0;" hidden="@(!IsOffline)">
    <strong>Offline:</strong> You are currently offline. Changes are stored locally and will persist.
</div>